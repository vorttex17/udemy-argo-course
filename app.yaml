apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webs
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/vorttex17/udemy-argo-course.git
    targetRevision: master
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true